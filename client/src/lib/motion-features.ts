// Loaded after first paint by <LazyMotion> so the animation engine isn't in the initial bundle.
// domAnimation covers animate/exit/whileInView/hover; no layout animations are used.
export { domAnimation as default } from 'framer-motion'
